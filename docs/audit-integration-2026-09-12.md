# 审查修复集成验收 · 2026-09-12

十张票均已在独立 worktree、`codex/audit-*` 分支和独立 Session 完成实现、提交、复核并合入本地 `main`。原[审查报告](project-audit-2026-09-12.md)保留修复前基准语境；[当前架构](current-architecture.html)已同步最终代码。

起始基准为 `b429580`（代码基于 `19323b0`）。九项代码票集成提交为 `6775ab0`，十票集成提交为 `a38e06c`。未执行 push、发布或线上部署；十个分支及工作树保留，交付时均干净。

## 逐票结果

| Ticket | 交付行为 | 实现提交 | main 合并提交 | 状态 |
|---|---|---|---|---|
| 01 | Exa 目标错误不改变密钥健康 | `172f104` | `8043050` | 已合并 |
| 02 | 慢 MCP 工具通过有界线程池调度 | `cb742a9` | `1f1aa25` | 已合并 |
| 03 | GitHub 可选认证接入共享密钥池 | `de0b739` | `d499aa0` | 已合并 |
| 04 | gh 重试共享绝对 deadline | `b7df416` | `3da9cba` | 已合并 |
| 05 | 密钥认证失败计数原子更新 | `a726364` | `ec4e8cb` | 已合并 |
| 06 | 显式坏配置提前报错并保留合法覆盖 | `f7b3c98` | `7bc7e4f` | 已合并 |
| 07 | Unicode 匹配映射回原文偏移 | `7b854ff` | `cddd8ca` | 已合并 |
| 08 | 预取正文只选择一次，响应与缓存共用 | `66feda8` | `7b796f7` | 已合并 |
| 09 | 删除无生产调用的旧抓取与正文回写链 | `3bcf721` | `6775ab0` | 已合并 |
| 10 | 同步当前架构说明与 MMD/SVG/HTML | `914fa46` | `a38e06c` | 已合并 |

所有票在开始实施时均无硬依赖，因此并行开始。共享文件仅影响集成顺序；最终文档依据已完成代码同步，没有遗留依赖票。

## 集成处理

- 01 与 05：目标错误跳过密钥健康写入；认证失败使用原子 UPDATE。组合探针确认三次并发认证失败仍为 `strikes=3 / invalid`。
- 03 与 04：解决 `registry.py` 的一处冲突，同时保留单个 `key` 和 `deadline=ctx.deadline`。密钥轮换与 gh 超时组合回归通过。
- 06、08、09：保留显式请求配置覆盖、唯一 `prefetched_bodies` 通道和 observer；仅删除已无生产调用的旧链。开启/关闭状态均得到同一正文，原配置不被修改。
- 09 的测试净减少 13 项：删除 18 项旧实现测试、新增 5 项，另迁移 6 项已有测试的公共断言到现行流程；排序、兼容参数和线程边界继续覆盖。

## 验证证据

| 检查 | 结果 |
|---|---|
| 各票失败复现、针对回归及独立复核 | 通过；每票先确认问题或旧图错误，再修复 |
| 最终共享代码全量回归 | **538 项通过，28.294 秒**，Windows / Python 3.13.13 / MCP 1.30.0 |
| 三组跨票组合探针 | 通过，无组合回归 |
| 隔离非 editable 安装 | 从 `6775ab0` 干净 Git 快照构建，安装到独立环境 |
| 仓库外 CLI 与 MCP stdio | CLI help/fetch help、初始化、12 个工具注册、list_sources 通过 |
| 安装内容完整性 | 59 个 Python 文件与提交快照逐文件 SHA-256 一致，无多余模块；工作树文本除换行规范外一致 |
| 文档与渲染 | 33 个节点 MMD/SVG 一致，14 个 HTML 引用及简易说明链接有效，浏览器分区/缩放检查通过 |
| 最终文档合并后 | 再次校验图源/尺寸/链接；与已验收代码相比仅文档变化 |
| Git | 所有票的实现提交均为 main 祖先；工作树及 diff 检查通过 |

全量回归：

```powershell
& .venv\Scripts\python.exe -B -X utf8 scripts/run_tests.py
```

最终隔离安装的协议验证：

```powershell
& .scratch\project-audit-2026-09-12\integration-install\Scripts\python.exe -B -X utf8 scripts/smoke_install.py
```

本机旧 `build/lib` 曾包含历史残留模块，直接增量构建会带入额外文件。最终安装验收使用干净 Git 快照，文件集合及内容均经过比对；没有把旧构建残留当成交付包。仅清理了已核对的旧 planner 构建副本，其他既有构建文件保留。

本地逐票验收、Session/worktree 路径及安装完整性证据位于忽略目录 `.scratch/project-audit-2026-09-12/`。验证使用模拟网络、临时 SQLite 和模拟凭据，没有使用真实密钥或付费 API；未执行 Linux/Python 3.10/3.14 矩阵、真实 provider 验收或生产压测。
