[project]
name = "multi-search-mcp"
version = "0.2.1"
description = "Aggregated search MCP server for the multi-search plugin."
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "beautifulsoup4>=4.12",
    "twikit-ng>=2.5",
]
